[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.backends._legacy:_Backend"

[project]
name = "zhuyin-decoder"
version = "0.1.0"
description = "偵測並轉換注音鍵盤誤打，讓 AI 看得懂忘記切輸入法的中文輸入"
requires-python = ">=3.10"

[project.scripts]
zhuyin-decode = "zhuyin_decoder.cli:main"
